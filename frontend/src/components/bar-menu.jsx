

const BarMenu = () => (
    <section className="bar-menu">
        <div className="container bar-menu__container">
            <ul className="bar-menu__list">
            </ul>
        </div>
    </section>
)

export default BarMenu;